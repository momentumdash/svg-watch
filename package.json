{
	"name": "svg-watch",
	"version": "0.1.0",
	"description": "Watch a folder for SVGs and run svgo on them.",
	"main": "index.js",
	"scripts": {
		"start": "gulp",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Momentum Dash",
	"license": "ISC",
	"dependencies": {
		"del": "^5.1.0",
		"gulp": "^4.0.2",
		"gulp-svgo": "^2.1.1"
	}
}
